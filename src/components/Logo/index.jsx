import styles from "./logo.module.scss";

const Logo = () => {
  return (
    <a className={styles.logo} href="/">
      <span>Oluwadara </span>
      Olasunkanmi
    </a>
  );
};

export default Logo;
